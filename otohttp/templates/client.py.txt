# Code generated by oto; DO NOT EDIT.

import requests
import json

class Client:
	"""Client provides access to the Firesearch API."""

	def __init__(self, endpoint="http://localhost:8888/api", apiKey=""):
		self.endpoint = endpoint
		self.apiKey = apiKey
		if self.endpoint == "":
			raise FieldError(field="endpoint", message="endpoint missing")

{{- range $service := .def.Services }}class {{ $service.Name }}:
	"""{{ format_comment_line $service.Comment }}"""

	def __init__(self, client):
		self.client = client
	{{- range $method := $service.Methods }}
	def {{ $method.NameLowerCamel }}(self, {{ $method.InputObject.ObjectNameLowerCamel }}):
		"""{{ format_comment_line $method.Comment }}"""
		url = "{}/{{ $service.Name }}.{{ $method.Name }}".format(self.client.endpoint)
		headers = {
			'Accept': 'application/json; charset=utf8',
			'Content-Type': 'application/json; charset=utf8',
			'X-API-Key': self.client.apiKey,
		}
		body = {{ $method.InputObject.ObjectNameLowerCamel }}
		{{- $authField := auth_field_for $.def $method }}
		{{- $authSpec := auth_spec_for $.def $method }}
		{{- if $authSpec }}
		auth_value = None
		if isinstance({{ $method.InputObject.ObjectNameLowerCamel }}, dict):
			auth_value = {{ $method.InputObject.ObjectNameLowerCamel }}.get("{{ $authField.NameLowerCamel }}")
			body = dict({{ $method.InputObject.ObjectNameLowerCamel }})
			body.pop("{{ $authField.NameLowerCamel }}", None)
		params = None
		cookies = None
		if auth_value:
			{{- if eq $authSpec.In "header" }}
			auth_header = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}auth_value
			headers["{{ $authSpec.Name }}"] = auth_header
			{{- end }}
			{{- if eq $authSpec.In "query" }}
			auth_query = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}auth_value
			params = {"{{ $authSpec.Name }}": auth_query}
			{{- end }}
			{{- if eq $authSpec.In "cookie" }}
			auth_cookie = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}auth_value
			cookies = {"{{ $authSpec.Name }}": auth_cookie}
			{{- end }}
		r = requests.post(url, json=body, headers=headers, params=params, cookies=cookies)
		{{- else }}
		r = requests.post(url, json=body, headers=headers)
		{{- end }}
		if r.status_code != 200:
			err = None
			try:
				j = r.json()
				if isinstance(j, dict):
					err = j.get('error')
			except ValueError:
				err = None
			if err:
				raise OtoError(message=err)
			raise OtoError(message="status code: {}".format(r.status_code))
		if r.text.strip() == "":
			return {}
		return r.json()
	{{- end }}
{{ end }}

class Error(Exception):
	"""Base class for exceptions in this module."""
	pass

class OtoError(Error):
	"""Exception raised for an error making the request.

	Attributes:
		message -- explanation of the error
	"""

	def __init__(self, message):
		self.message = message

class FieldError(Error):
	"""Exception raised for missing fields.

	Attributes:
		field -- field which the error occurred
		message -- explanation of the error
	"""

	def __init__(self, field, message):
		self.field = field
		self.message = message
