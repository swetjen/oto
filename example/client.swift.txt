//  Code generated by oto; DO NOT EDIT.

import Foundation

class OtoClient {
	var endpoint: String
	init(withEndpoint url: String) {
		self.endpoint = url
	}
}

{{- range $service := .def.Services }}
{{ format_comment_text $service.Comment }}class {{ $service.Name }} {
	var client: OtoClient
	init(withClient client: OtoClient) {
		self.client = client
	}
{{- range $method := $service.Methods }}
	{{ format_comment_text $method.Comment }}	func {{ camelize_down $method.Name }}(withRequest {{ camelize_down $method.InputObject.TypeName }}: {{ $method.InputObject.TypeName }}, completion: @escaping (_ response: {{ $method.OutputObject.TypeName }}?, _ error: Error?) -> ()) {
		var url = "\(self.client.endpoint)/{{ $service.Name }}.{{ $method.Name }}"
		{{- $authField := auth_field_for $.def $method }}
		{{- $authSpec := auth_spec_for $.def $method }}
		{{- if $authSpec }}
		{{- if eq $authSpec.In "query" }}
		if let authValue = {{ camelize_down $method.InputObject.TypeName }}.{{ camelize_down $authField.Name }}, !authValue.isEmpty {
			let authQuery = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue
			var components = URLComponents(string: url)
			var items = components?.queryItems ?? []
			items.append(URLQueryItem(name: "{{ $authSpec.Name }}", value: authQuery))
			components?.queryItems = items
			if let updatedURL = components?.url?.absoluteString {
				url = updatedURL
			}
		}
		{{- end }}
		{{- end }}
		var request = URLRequest(url: URL(string: url)!)
		request.httpMethod = "POST"
		request.addValue("application/json; charset=utf-8", forHTTPHeaderField: "Content-Type")
		request.addValue("application/json; charset=utf-8", forHTTPHeaderField: "Accept")
		{{- if $authSpec }}
		{{- if eq $authSpec.In "header" }}
		if let authValue = {{ camelize_down $method.InputObject.TypeName }}.{{ camelize_down $authField.Name }}, !authValue.isEmpty {
			let authHeader = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue
			request.addValue(authHeader, forHTTPHeaderField: "{{ $authSpec.Name }}")
		}
		{{- end }}
		{{- if eq $authSpec.In "cookie" }}
		if let authValue = {{ camelize_down $method.InputObject.TypeName }}.{{ camelize_down $authField.Name }}, !authValue.isEmpty {
			let authCookie = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue
			request.addValue("{{ $authSpec.Name }}=" + authCookie, forHTTPHeaderField: "Cookie")
		}
		{{- end }}
		{{- end }}
		var jsonData: Data
		do {
			jsonData = try JSONEncoder().encode({{ camelize_down $method.InputObject.TypeName }})
		} catch let err {
			completion(nil, err)
			return
		}
		request.httpBody = jsonData
		let session = URLSession(configuration: URLSessionConfiguration.default)
		let task = session.dataTask(with: request) { (data, response, error) in
			if let err = error {
				completion(nil, err)
				return
			}
            if let httpResponse = response as? HTTPURLResponse {
                if (httpResponse.statusCode != 200) {
                    let err = OtoError("\(url): \(httpResponse.statusCode) status code")
                    completion(nil, err)
                    return
                }
            }
			guard let responseData = data, responseData.count > 0 else {
				completion({{ $method.OutputObject.TypeName }}(), nil)
				return
			}
			if let responseText = String(data: responseData, encoding: .utf8)?.trimmingCharacters(in: .whitespacesAndNewlines),
			   responseText == "null" || responseText == "" {
				completion({{ $method.OutputObject.TypeName }}(), nil)
				return
			}
			var {{ camelize_down $method.OutputObject.TypeName }}: {{ $method.OutputObject.TypeName }}
			do {
				{{ camelize_down $method.OutputObject.TypeName }} = try JSONDecoder().decode({{ $method.OutputObject.TypeName }}.self, from: responseData)
			} catch let err {
				completion(nil, err)
				return
			}
			completion({{ camelize_down $method.OutputObject.TypeName }}, nil)
		}
		task.resume()
	}
{{- end }}
}
{{ end }}

{{- range $object := .def.Objects }}
{{ format_comment_text $object.Comment }}struct {{ $object.Name }}: Encodable, Decodable {
{{- range $field := $object.Fields }}
	{{ format_comment_text $field.Comment }}	var {{ camelize_down $field.Name }}: {{ if $field.Type.IsObject }}{{ $field.Type.TypeName }}{{ else }} {{ $field.Type.SwiftType }}{{ end }}?
{{- end }}
{{- $fields := non_auth_fields $object }}
{{- if ne (len $fields) (len $object.Fields) }}

	enum CodingKeys: String, CodingKey {
{{- range $field := $fields }}
		case {{ camelize_down $field.Name }}
{{- end }}
	}
{{- end }}
}
{{ end }}

struct OtoError: LocalizedError
{
    var errorDescription: String? { return message }
    var failureReason: String? { return message }
    var recoverySuggestion: String? { return "" }
    var helpAnchor: String? { return "" }

    private var message : String

    init(_ description: String) {
        message = description
    }
}
