// Code generated by oto; DO NOT EDIT.

'use strict';

/**
 * Errors thrown by client methods include network failures or server errors.
 */

{{- range $service := .def.Services }}
{{- range $method := $service.Methods }}
/**
 * @typedef {Object} {{ $service.Name }}{{ $method.Name }}Request
{{- range $object := $.def.Objects }}
{{- if eq $object.Name $method.InputObject.CleanObjectName }}
{{- range $field := $object.Fields }}
 * @property {{ "{" }}{{ $field.Type.JSType }}{{ if $field.Type.Multiple }}[]{{ end }}{{ "}" }} {{ $field.NameLowerCamel }}{{ if ne $field.Comment "" }} - {{ format_comment_line $field.Comment }}{{ end }}
{{- end }}
{{- end }}
{{- end }}
 */
/**
 * @typedef {Object} {{ $service.Name }}{{ $method.Name }}Response
{{- range $object := $.def.Objects }}
{{- if eq $object.Name $method.OutputObject.CleanObjectName }}
{{- range $field := $object.Fields }}
 * @property {{ "{" }}{{ $field.Type.JSType }}{{ if $field.Type.Multiple }}[]{{ end }}{{ "}" }} {{ $field.NameLowerCamel }}{{ if ne $field.Comment "" }} - {{ format_comment_line $field.Comment }}{{ end }}
{{- end }}
{{- end }}
{{- end }}
 */
{{- end }}
{{- end }}

const client = {
{{- range $service := .def.Services }}
	{{- if ne $service.Comment "" }}
	/**
	 * {{ format_comment_line $service.Comment }}
	 */
	{{- end }}
	{{ $service.Name }}: {
		{{- range $method := $service.Methods }}
		/**
		{{- if ne $method.Comment "" }}
		 * {{ format_comment_line $method.Comment }}
		{{- end }}
		 * @param {{ "{" }}{{ $service.Name }}{{ $method.Name }}Request{{ "}" }} [request]
		 * @returns {Promise<{{ $service.Name }}{{ $method.Name }}Response>}
		 */
		async {{ camelize_down $method.Name }}(request) {
			const headers = {
				'Accept':		'application/json',
				'Accept-Encoding':	'gzip',
				'Content-Type':		'application/json',
			}
			let url = '/oto/{{ $service.Name }}.{{ $method.Name }}'
			request = request || {}
			const body = Object.assign({}, request)
			{{- $authField := auth_field_for $.def $method }}
			{{- $authSpec := auth_spec_for $.def $method }}
			{{- if $authSpec }}
			const authValue = request.{{ $authField.NameLowerCamel }}
			if (authValue) {
				{{- if eq $authSpec.In "header" }}
				const authHeader = {{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue
				headers['{{ $authSpec.Name }}'] = authHeader
				{{- end }}
				{{- if eq $authSpec.In "query" }}
				const sep = url.includes('?') ? '&' : '?'
				url = url + sep + encodeURIComponent('{{ $authSpec.Name }}') + '=' + encodeURIComponent({{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue)
				{{- end }}
				{{- if eq $authSpec.In "cookie" }}
				const cookieValue = encodeURIComponent({{ if ne $authSpec.Prefix "" }}"{{ $authSpec.Prefix }} " + {{ end }}authValue)
				document.cookie = '{{ $authSpec.Name }}=' + cookieValue + '; path=/'
				{{- end }}
			}
			if (Object.prototype.hasOwnProperty.call(body, '{{ $authField.NameLowerCamel }}')) {
				delete body.{{ $authField.NameLowerCamel }}
			}
			{{- end }}
			const response = await fetch(url, {
				method: 'POST',
				headers: headers,
				{{- if $authSpec }}
				{{- if eq $authSpec.In "cookie" }}
				credentials: 'same-origin',
				{{- end }}
				{{- end }}
				body: JSON.stringify(body)
			})
			const text = await response.text()
			let json = null
			if (text) {
				try {
					json = JSON.parse(text)
				} catch (e) {
					if (!response.ok) {
						throw new Error(`{{ $service.Name }}.{{ $method.Name }}: ${response.status} ${response.statusText}`)
					}
					throw e
				}
			}
			if (!response.ok) {
				if (json && json.error) {
					throw new Error(json.error)
				}
				throw new Error(`{{ $service.Name }}.{{ $method.Name }}: ${response.status} ${response.statusText}`)
			}
			return json || {}
		}
		{{- end }}
	},
{{- end }}
}
